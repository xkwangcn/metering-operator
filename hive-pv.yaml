apiVersion: v1
kind: PersistentVolume
metadata:
  name: hive-nfs-test1
  namespace: metering
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
    - ReadWriteOnce
    - ReadOnlyMany
  nfs:
    path: /var/export/nfs_hive
    server: *.*.*.*
