apiVersion: metering.openshift.io/v1
kind: MeteringConfig
metadata:
  name: operator-metering
spec:
  storage:
  type: "hive"
  hive:
    type: "sharedPVC"
    sharedPVC:
      claimName: "metering-nfs"
